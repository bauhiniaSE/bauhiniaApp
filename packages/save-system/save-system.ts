export class Save {
  public saveToDatabase(map: any) {
    const data = JSON.stringify(map);
    //pushToBase(data);
  }
  //   loadToDatabase(id) {
  //     const map = getFromDatabase(id);
  //     return JSON.parse(map);
  //   }
}
