export interface IAuthService {

  }

  function isAdmin (id:string) : boolean{

    return true;
  }

  function logIn (id:string, pass:String) : User{


  }

  function signIn (id:string, pass:string) :User{

    
  }