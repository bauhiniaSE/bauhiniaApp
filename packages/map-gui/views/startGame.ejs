<html>

<head>
    <style>
        @import url(https://fonts.googleapis.com/css?family=Roboto:300);
        /* Style The Dropdown Button */
        .flex {
            display: flex;
            justify-content: center;
        }

        .flex-item+.flex-item {
            margin-left: 10px;
        }



        .dropbtn {
            font-family: "Roboto", sans-serif;
            background: #fdfffb;
            box-shadow: 0 0 10px 0 #a4ccb9, 0 3px 3px 0 #24616b;
            margin-top: 100px;
            margin-left: 10px;
            margin-right: 10px;
            color: #24616b;
            padding: 25px;
            font-size: 25px;
            border: none;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            width: 200px;

        }

        /* The container <div> - needed to position the dropdown content */
        .dropdown {
            position: center;
            display: inline-block;
        }

        /* Dropdown Content (Hidden by Default) */
        .dropdown-content {
            font-family: "Roboto", sans-serif;
            margin-left: 10px;
            margin-right: 10px;
            display: none;
            position: absolute;
            background-color: #e5edf7;
            width: 200px;
            text-align: center;
            box-shadow: 0 0 10px 0 #a4ccb9, 0 3px 3px 0 #24616b;
            z-index: 1;
        }

        /* Links inside the dropdown */
        .dropdown-content a {
            color: black;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
        }

        /* Change color of dropdown links on hover */
        .dropdown-content a:hover {
            background-color: #24616b;
            color: white;
        }

        /* Show the dropdown menu on hover */
        .dropdown:hover .dropdown-content {
            display: block;
        }

        /* Change the background color of the dropdown button when the dropdown content is shown */
        .dropdown:hover .dropbtn {
            background-color: #a4ccb9;
            color: #133f46;
        }


        body {
            background-image: -webkit-linear-gradient(0deg, #045714, #24616b);
            font-family: "Roboto", sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
    </style>
    <title>Start Game</title>
</head>

<body>
    <div class="flex">
        <form>
            <div class="dropdown">
                <button class="dropbtn">Start Game</button>
                <div class="dropdown-content">
                    <a href="#">Blueprint 1</a>
                    <a href="#">Blueprint 2</a>
                    <a href="#">Blueprint 3</a>
                </div>
            </div>

            <div class="dropdown">
                <button class="dropbtn">Load Game</button>
                <div class="dropdown-content">
                    <a href="#">Game 1</a>
                    <a href="#">Game 2</a>
                    <a href="#">Game 3</a>
                </div>
            </div>
        </form>
    </div>

</body>

</html>